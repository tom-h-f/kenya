.env
config/accounts.yaml
.venv/
.git/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
data/
*.parquet
# Runtime state lives in the named volume, never baked into the image - a fresh
# volume seeds from the image layer, so a stale accounts.db / dynamic_targets.json
# would otherwise leak in on first boot.
state/
accounts.db
